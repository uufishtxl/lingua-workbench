# ------------------------------------
# Frontend Environment Variables
# ------------------------------------
#
# 给 Axios 用的：告诉前端，请求发给本地代理 '/api'
# 开发阶段请保持为 '/api'，这样浏览器才会把请求发给 Vite (5173端口)，从而触发 Vite 的代理层转发。
VITE_API_BASE_URL=/api

# 给 Vite 代理用的：vite.config.ts
VITE_DEV_BACKEND_URL=http://127.0.0.1:8000